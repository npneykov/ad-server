# Admin key for protected routes (e.g. /admin, /admin/ads)
ADMIN_KEY=changeme123

# SQLite by default – for local testing/deployment
DATABASE_URL=sqlite:///./adserver.db

# Optional: Host and port override (default: 0.0.0.0:8000)
# HOST=0.0.0.0
# PORT=8000

# Optional: Uvicorn log level
# LOG_LEVEL=info
